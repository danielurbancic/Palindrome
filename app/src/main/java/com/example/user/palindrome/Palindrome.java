package com.example.user.palindrome;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.EditText;

public class Palindrome extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_palindrome);




        EditText palindrome_in = (EditText);
        EditText button = (EditText);
        EditText ausgabe_ispal = (EditText);
        EditText ausgabe_nopal = (EditText);
        EditText ausgabe_space = (EditText);
        EditText ausgabe_wordlenght = (EditText);
        


    }
}
